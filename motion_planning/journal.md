# Journal

## 7/7

- install vcpkg under `/home/ryuuma/libraries/cpp-libs/vcpkg`
- having `eigen` build & run
- got `tcl` and `polyscape` to work
- started the car simulation
  - a lot of questions where we put what
  - currently at update algorithm for a car (front axis, back axis)

## 7/8

- finish `step` function -> write test for it
- wrote a small test for the `car` function
- write the visualization for the `car` and `pos` of `car`
- we only use 2d, but it starts floating, need to check physics

## 7/10

- fixed problem with "physics", `y-axis` is up in `polyscope`, bit weird to me but okay
- 